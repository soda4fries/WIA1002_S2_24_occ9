package Tutorial;

/**
 * Name: Heng Xin Phei
 * Occ: 9
 * Date: 17/3/2024
 * Objective: Q9: When the compiler encounters a generic class, interface, or method with an unbound type 
                  parameter, such as <T> or <E>, it replaces all occurrences of the type parameter with what 
                  type? 
*             Q10: When the compiler encounters a generic class, interface, or method with a bound type 
                   parameter, such as <T extends Number> or <E extends Comparable>, it replaces all 
                   occurrences of the type parameter with what type? 
 */
public class Q9Q10 {
    
    //Q9: Object type
    //Q10: Bound that is applied to the parameter (e.g: Number, Comparable)
    
}
